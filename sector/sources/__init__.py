"""원천 어댑터 — 바깥 세계와 이 저장소가 만나는 유일한 층.

각 모듈은 원천 하나를 담당하고 **파싱된 값 객체**를 돌려준다. 집계·스코어링은
원천의 필드명·문자열 포맷을 알지 못한다.

🔴 여기서 얻은 KRX **원천** 데이터는 저장소·HF 에 올리지 않는다. 로컬
   `data/raw/`(gitignore) 까지다 — 약관 제11조② (→ ADR-SC-0006).
"""

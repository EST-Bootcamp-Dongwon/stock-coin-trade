# 출처 표기 (NOTICE)

## 1. 이 저장소의 코드는 전부 직접 작성한 것이다

이 저장소는 강사님 원본 [`edumgt/stock-coin-trade`](https://github.com/edumgt/stock-coin-trade)
의 fork 로 **출발했으나**, 2026-09-11 에 산출물을 섹터 ETF 레이더로 재지정하면서
**이력을 orphan 으로 재시작했다.** 그때 강사님 코드를 새 이력에 담지 않았다.

→ 근거: [ADR-SC-0005](docs/decisions/0005-산출물-재지정과-이력-재시작.md)

| | |
| --- | --- |
| 새 이력에 **없는 것** | `python-stock-backend/` `frontend/` `database/` `docker/` `captures/` `scripts/` `.dockerignore` `.github/` — 전부 강사님 저작물 |
| 새 이력에 **있는 것** | `backend/`(v2.0 Django · 동결) · `sector/` · `batch/` · `dashboard/` · `docs/` — 전부 본인 저작물 |

워킹트리에서 파일을 지워도 이력에서는 사라지지 않기 때문에, 선별 제거가 아니라
**이력 재시작**을 택했다. 그 판단의 근거와 대안 비교는 ADR-SC-0005 에 있다.

### 강사님 원본은 어디에 있나

지웠다고 사라지는 것이 아니라 **읽을 곳을 옮겼을 뿐**이다.

| 위치 | 형태 |
| --- | --- |
| `upstream` 원격 | fetch 전용. push URL 은 `DISABLED://` 로 막혀 있다 |
| [`edumgt/stock-coin-trade`](https://github.com/edumgt/stock-coin-trade) | 원본 그대로 |
| 모노레포 `learning/th03-stock-coin-trade/lecture/` | 서브모듈 (읽기 전용) |

### 라이선스

원본에 LICENSE 파일이 없어 **제3자 재사용에 원저작자 허가가 필요한 상태**였다.
v3.0 이력에는 원본 코드가 들어 있지 않으므로 이 저장소를 공개하는 데에는
그 제약이 걸리지 않는다. 원본 코드가 필요하면 위의 원본 저장소에서 직접 받고,
그때의 이용 조건은 원저작자에게 확인한다.

수업 중 구두로 자유로운 사용·수정·개인 프로젝트화를 허가받았으나,
**구두 허가를 서면 라이선스로 간주하지 않는다.**

---

## 2. 🔴 데이터 출처 — 한국거래소 통계정보

이 프로젝트의 시세·지수 데이터는 **한국거래소(KRX) Open API** 에서 받는다.

> **한국거래소 통계정보**

KRX Open API 이용약관이 정하는 것:

| 조항 | 내용 | 이 프로젝트의 준수 |
| --- | --- | --- |
| 제10조③ | 출처 표시 의무 | **모든 화면 하단에 "한국거래소 통계정보" 를 상시 표시한다** |
| 제11조② | 제공받은 정보를 **제3자에게 제공할 수 없다** | 원천 데이터를 저장소·Hugging Face 에 올리지 않는다. 로컬 `data/`(gitignore)에만 둔다. 공유하는 것은 **섹터 집계·점수 등 파생값뿐**이다 |
| 제6조② | **비상업 목적만** | 광고·유료 기능이 없는 팀 내부 도구다 |

→ 근거: [ADR-SC-0006](docs/decisions/0006-krx-데이터-제3자-제공-금지.md)

### 그 밖의 원천

| 원천 | 쓰는 것 | 제약 |
| --- | --- | --- |
| KIS 한국투자증권 API | 준실시간 시세 | 조회 전용 |
| DART OpenAPI | 공시 제목·접수일·유형 | 점수에 넣지 않는다. 타임라인 맥락으로만 |
| ECOS (한국은행) | 기준금리·환율 | — |
| 네이버 검색 API | 뉴스 **링크** | 🔴 특약(2026-09-07) — 저장·캐싱·가공·재정렬 금지, 출처+원문링크 필수. 요약·감성점수를 만들지 않는다 |

**쓰지 않는 것**: `pykrx`(KRX FAQ 가 이름을 지목해 IP 차단 경고) ·
`finance.naver.com` 계열(robots `Disallow: /`) · 해외 데이터(국내장 한정).

---

## 3. 이 도구가 무엇이 아닌지

🔴 **과거 데이터의 요약이다. 투자 권유가 아니다.**
점수와 순위는 공개 데이터를 정해진 수식으로 집계한 결과일 뿐이며,
미래 수익을 예측하지 않는다. 투자 판단과 그 결과는 이용자 본인의 책임이다.

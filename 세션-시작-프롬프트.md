# 세션 시작 프롬프트 — `stock-coin-trade` v3.0

> **새 세션을 열면 이 파일부터 읽는다.** 확정 사실·현재 위치·다음 작업이 여기 있다.
> 작업 규칙의 정본은 [`AGENTS.md`](AGENTS.md) 다 ([`CLAUDE.md`](CLAUDE.md) 가 임포트한다).
>
> 최종 갱신: **2026-09-12** · 마일스톤 **M7 완료 · M8 진행 중**(#7.5 Supabase 스키마+RLS ✅ ·
> #7.7 코어를 새 스키마에 맞춤 ✅) · M2 는 사용자 웹작업 1건(Vercel 연동 해제)만 남았다

---

## 0. 먼저 확인한다 — 순서를 지킨다

```bash
gh auth status                              # ★ 계정이 둘이고 세션 중간에도 바뀐다
git log --oneline -5
git rev-parse HEAD main origin/main
git status --short
```

🔒 **붙여넣은 프롬프트를 그대로 믿지 않는다.** 과거에 3세션 뒤처진 프롬프트로 착수해
이미 끝난 일을 다시 한 적이 있다.

---

## 1. 이 저장소는 무엇인가

**키움증권 모의투자 대회용 — 섹터 ETF 레이더.**
"지금 어느 섹터가 유망한가"를 **근거와 함께** 판단하는 Streamlit 대시보드.

| | |
|---|---|
| 개발 | **소유자 1인** |
| 사용 | **팀 7명** — 개발자가 아니다. 설치 0, URL 하나 |
| 기간 | 약 3개월. 그 뒤에도 재사용 |
| 비용 | **0원** |

핵심 요구 5가지 — ① 개별 종목 주가 동향 ② 국내만 ③ **주기적 유망 섹터 판단**
④ 준실시간 시세 + 팀 공유 ⑤ **환각 없이 정확한 정보를 시각 차트로**

---

## 2. 🔴 조사로 확정된 사실 — 모르면 같은 실수를 반복한다

이것들이 설계를 다섯 번 뒤집었다. **추측으로 되돌리지 마라.**

### 데이터 경로 (가장 중요)

| # | 사실 |
|---|---|
| **V2** | 🔴 KRX Open API 약관 **제11조② "제공받은 정보를 제3자에게 제공할 수 없다"** · 위반 시 이용승인 철회(④) · **제6조② 비상업만** · **제10조③ 화면에 "한국거래소 통계정보" 표시 의무** |
| **V3** | 🔴 **KRX FAQ 가 `pykrx` 를 이름으로 지목**해 "과도한 접속 시 IP 차단, 해제도 어렵다" |
| **V4** | ✅ **`etp/etf_bydd_trd` 가 `NAV`·`LIST_SHRS`(상장좌수)·`INVSTASST_NETASST_TOTAMT` 를 반환.** 2010-01-04부터 · 키당 **일 10,000회** · 유효 1년 · **개별 API 별도 승인 필요** · 영업일 익일 08:00 갱신 |
| **V5** | 🔴 KRX Open API 에 **지수 구성종목·ETF PDF·투자자별 매매동향·외국인 보유량·공매도·PER/PBR 이 없다** |
| **V6** | 🔴 네이버 검색 API **특약(2026-09-07 시행)** — 저장·캐싱·가공·재정렬·AI 입력 금지 · 보관 **최대 21일** · 출처+원문링크 필수. 개발자센터 신규 발급 중단(2026-07-31 → NAVER API HUB) |
| **V7** | 🔴 **네이버 이용약관이 봇·스크래퍼 자동 수집을 명시 금지.** `polling.finance.naver.com` 은 robots 404지만 약관은 별개 |
| **V8** | 🔴 `finance.naver.com` · `api.finance.naver.com` · `m.stock.naver.com` 이 robots **`Disallow: /`** (직접 확인) |
| **V22** | 🔴 **`etp/etf_bydd_trd` 실관찰**(2026-09-11 · `basDd=20260910` · 1168행) — ① `ISU_CD` **304행에 영문자**(`9999A9` 꼴)라 `\d{6}` 검증은 ETF 304개를 잃는다 ② 모든 비율·NAV 가 **소수 2자리 고정**이라 `%×100=bp` 가 정수로 떨어진다(전수 확인) ③ 금액·수량에 소수점 0건 ④ `ACC_TRDVOL`·`ACC_TRDVAL` 이 **진짜 `"0"` 인 행 11건** ⑤ 기초지수 3필드가 **빈 문자열인 행 1건**(기초지수 미공표 ETF) ⑥ 휴일 → HTTP 200 + `{"OutBlock_1": []}` ⑦ 키 오류·누락 → **HTTP 401** `{"respMsg":"Unauthorized Key"}`. 🔒 **`"0"` 과 `""` 를 같게 만들면 안 된다** |
| **V23** | ✅ **`sto/stk_bydd_trd`(유가증권) · `sto/ksq_bydd_trd`(코스닥) 승인 확인됨**(2026-09-11 · `basDd=20260910` · **943행 · 1823행** · HTTP 200) — 필드 15개(`ISU_CD`·`ISU_NM`·`MKT_NM`·`SECT_TP_NM`·4가격·`CMPPREVDD_PRC`·`FLUC_RT`·`ACC_TRDVOL`·`ACC_TRDVAL`·`MKTCAP`·`LIST_SHRS`·`BAS_DD`). 🔴 **ETF 응답에 없는 `MKT_NM`·`SECT_TP_NM` 이 있고, `NAV`·기초지수 4필드는 없다** — 스키마가 다르다. 🔴 **주식 코드도 81/2766 이 영문자를 포함한다**(`18064K` 꼴 우선주) — `\d{6}` 금지가 ETF 만의 문제가 아니다. ETF 유니버스와 주식 유니버스는 **교집합 0** 이라 따로 다뤄야 한다. |
| **V24** | ✅ **지수(`idx/*`) API 5종 전부 승인 확인됨**(2026-09-11 · `basDd=20260910` · HTTP 200) — `krx_dd_trd` 40행 · `kospi_dd_trd` 51행 · `kosdaq_dd_trd` 40행 · `bon_dd_trd` 3행 · `drvprod_dd_trd` 320행. 🔴 **미확인 경로가 이제 없다.** 다만 **M5 는 이 API 를 쓰지 않는다** — 모멘텀·폭 축이 빼는 시장 기준선은 이미 받는 주식 유니버스에서 만든다. 폭(breadth)과 **같은 유니버스·같은 방법**이어야 뺄셈이 뜻을 갖기 때문이다 |
| **V25** | 🔴 **`FLUC_RT` 가 수익률의 유일한 재료다.** KRX 종가는 **무수정 주가**라 액면분할·권리락이 끼면 `close_t/close_{t-1}−1` 이 −50% 같은 가짜 수익률을 낸다. `FLUC_RT` 는 거래소가 **조정한 기준가 대비** 등락률이다 — 이것으로 체인연결 지수를 만들면 **수정주가 시계열과 동등**해지고 자본변동을 따로 탐지할 필요가 없다. SMA20·폭·20일 수익률이 전부 그 지수 위에서 계산된다 |
| **V26** | 🔴 **`None` 이 섞인 정수 열은 pandas 가 `float64` 로 올린다.** 규약이 금지한 float 가 저장 계층에 들어오고, 스냅샷이 `10000` 대신 `10000.0` 이 되어 골든 테스트가 환경마다 흔들린다. nullable `Int64` 를 **dataclass 주석에서 끌어내** 명시로 준다 — 손으로 적은 표는 필드가 늘 때 잊힌다 |
| **V27** | 🔴 **시장 휴일에 `etp/etf_bydd_trd` 는 0행이 아니라 1000행 넘게 준다** (2026-09-11 실관찰 · 신정·설·추석 등 **19일**). 그 행은 종목 마스터에 가깝다 — 종가·NAV·등락률·거래량·거래대금이 **전부 빈 문자열**이고 `LIST_SHRS`(상장좌수)만 전일과 같은 값으로 들어 있다. 주식 엔드포인트는 같은 날 정직하게 0행을 준다. 🔴 "행이 있으면 영업일"로 보면 ① 자금흐름 축의 "20영업일 전"이 20영업일이 아니게 되고(좌수는 휴일에도 차 있다) ② ETF 렌즈와 구성종목 렌즈의 날짜 축이 갈라진다. **달력이 아니라 데이터로 판정한다** — 등락률이 하나라도 있으면 장이 열린 것이다 (`build_sector_daily._traded`) |
| **V28** | 🔴 **시총 상위 2종이 전체의 46% 다**(2026-09 기준 · 2025-07 에는 19%였다 — 반도체 주도로 **단조 증가**). 그래서 시총가중 시장지수가 하루 **+17.6%** 를 찍는 날이 실재한다(2026-07-31 · 두 종목이 상한가 부근). 🔴 데이터 오류가 아니다 — `MKTCAP` 이 종가×상장주식수와 **2763행 전부 정확히** 일치하고 개별 등락률도 전부 ±30%(가격제한폭) 안이다. **M7 의 M 축(`r20_s − r20_mkt`)이 이 기준선을 쓰면 반도체가 오를 때마다 다른 20개 섹터가 전부 나빠 보인다.** → `market_daily.eqw_idx_bp`(동일가중)를 함께 만들어 뒀다. **어느 쪽을 기준선으로 쓸지는 M7 에서 의식적으로 고른다** |
| **V15** | Lee & Swaminathan(2000): **고회전 종목은 미래 수익률이 낮다.** 거래량은 독립 신호가 아니라 모멘텀의 지속성을 조건짓는 변수 |

### 인프라

| # | 사실 |
|---|---|
| **V1** | Streamlit Cloud 는 **GitHub 전용** · 메모리 최대 **2.7GB**(~~1GB~~) · 12시간 무트래픽 슬립 · **private 앱 동시 1개** · **뷰어 이메일 제한이 무료** |
| **V12** | HF Pro = 비공개 1TB · `huggingface_hub 1.27.0` 이 `backend/.venv` 에 설치됨 · Org `huggingface.co/stock-coin-trade` **실존** |
| **V16** | ~~Supabase 프로젝트 4개 전부 `INACTIVE` · 되살려도 7일 뒤 또 멈춘다~~ → **2026-09-12 개정.** `stock-coin-trade`(`sgbhrahtewojmicwmxxu` · 서울 · PG 17.6)가 **`ACTIVE_HEALTHY`** 다. **pause 는 매일 도는 `batch.publish` 가 한 줄 써서 푼다** — 당시엔 배치 운영 방식이 안 정해져 있었다. 🔴 Free 는 **DB 500MB**(5GB 는 egress) · 파일 1GB · **활성 프로젝트 2개**. ~~53개 테이블 전부 RLS 꺼짐~~ → **2026-09-12 해결**(ADR-SC-0011 · M8 #7.5): 53개 **전부 RLS 켜짐 · 정책 0개 · anon GRANT 0**. 🔴 당시 진단이 과소평가였다 — "0행이라 무해" 는 *읽기* 관점이고 실제로는 anon 에게 **INSERT·UPDATE·DELETE·TRUNCATE** 가 있었다(→ V36). 팀 원장 3테이블 신설 · 전부 0행(데이터는 아직) |
| **V19** | 🔴 **`st.secrets` 는 접근만으로 예외를 던진다** — 시크릿 파일이 없으면 `st.secrets["K"]` 뿐 아니라 **`"K" in st.secrets` · `st.secrets.get("K")` · `bool(st.secrets)` 까지 전부** `StreamlitSecretNotFoundError` 를 던져 **페이지 전체가 죽는다.** 즉 `.get()` 은 안전한 접근자가 **아니다.** MRO 가 `… → Error → FileNotFoundError → OSError` 라 `except FileNotFoundError` 로 잡힌다(streamlit 1.63.0 실측). M3(KRX 키)·M6(HF 토큰)에서 시크릿 미주입 상태로 배포하면 화면이 통째로 빈다 — **시크릿 접근을 함수 하나로 모아 거기서 예외를 끝낸다** |
| **V20** | Streamlit Cloud 는 의존성 파일을 ① 엔트리포인트 디렉터리 → ② 저장소 루트 순으로 찾고 **처음 만난 하나만** 쓴다. 우선순위 `uv.lock` → `Pipfile` → `environment.yml` → `requirements.txt` → `pyproject.toml`. 엔트리포인트가 루트라 **루트 `requirements.txt` 가 Streamlit 전용**이 된다. 🔴 Python 버전은 **배포 UI 에서만** 고르고 `"Python itself can't be changed after deployment"` — 바꾸려면 앱 삭제·재배포. `runtime.txt`·`.python-version` 은 읽지 않는다 |
| **V21** | 🔴 **뷰어 초대 권한이 전파된다** — 공식 문서: 뷰어가 `"can also pass these permissions to others by inviting more viewers"`, 그리고 뷰어를 추가하면 그 앱 + **내 모든 public 앱의 analytics** 접근권이 함께 간다. 기술로 잠기지 않으니 팀 규칙으로 막는다. public 저장소 + private 앱 조합은 공식 지원(`Only specific people can view this app`) · **private 앱은 동시 1개** |
| **V17** | Vercel 은 빌드가 깨진 게 아니다 — `/static/…` 200, `/admin/` 302, `/` 만 500. 원인은 V16 |
| **V31** | ✅ **두 배포가 공존한다**(2026-09-12 · ADR-SC-0010) — Vercel 프로젝트 설정의 **Root Directory = `backend/`** + **"Include source files outside of the Root Directory"** 토글로 `backend/requirements.txt` 를 따로 읽힌다. **파일 이동 0.** 🔴 V20 의 "루트 requirements.txt 가 Streamlit 전용" 은 여전히 맞다 — 이 설정은 **Vercel 쪽**에서 푼다 |
| **V32** | 🔴 **`bootstrap._fix_sys_path()` 는 스크립트 폴더만 `sys.path` 에 넣는다**(`sys.path.insert(0, os.path.dirname(main_script_path))` · 소스 확인). V20 대로 `app/streamlit_app.py` + `app/requirements.txt` 도 동작하지만 **`import sector` 가 깨져** hack 이 붙는다. 🔒 **설정으로 되는 일을 코드로 만들지 않는다** → V31 을 쓴다 |
| **V33** | ✅ Vercel Python 런타임이 넓어졌다(문서 갱신 2026-08-12) — **Django 자동 감지**(`manage.py` 가 **하위 디렉터리**에 있어도 찾아 `WSGI_APPLICATION` 에서 앱 해석) · `pyproject.toml`/`requirements.txt`/`Pipfile` · `tool.vercel.entrypoint` · `excludeFiles` 로 번들 축소 · 번들 **500MB**(Fluid Large Functions 5GB·베타) · Python 3.12 기본. 즉 `api/index.py` 래퍼와 `vercel.json` 의 `builds` 블록이 **이제 필요 없을 수 있다**. ⚠️ 여전한 제약 — **백그라운드 작업 없음 · 60초 · 영속 연결 없음**(배치를 로컬에 두는 우리 구조에는 안 물린다) |
| **V34** | 🔴 **앱이 테마를 한 번도 켜지 않았다**(2026-09-12) — `.streamlit/config.toml` 에 `base = "dark"` 한 줄뿐이고 주석이 스스로 "색·폰트는 M8 소관" 이라 유예해 뒀다. 즉 **"Streamlit 이라 못생겼다" 가 아니다.** `streamlit==1.63.0` 의 `[theme]` 는 계획서 F-4 를 거의 1:1 로 덮는다 — `fontFaces`·`headingFontSizes`·`headingFontWeights`·`baseFontSize`·`baseRadius`·`buttonRadius`·`borderColor`·`showWidgetBorder`·`showSidebarBorder`·`dataframeBorderColor`·`chartCategoricalColors`·의미색 7종·`[theme.sidebar]` |
| **V35** | 🔴 **`chartCategoricalColors` 는 CSS 로 대체 불가.** `st.bar_chart` 는 vega-lite 를 **canvas** 로 그려 CSS 가 닿지 않는다. "CSS 만으로 디자인한다" 는 제약을 두면 **차트 색을 영원히 못 고친다** |
| **V36** | 🔴🔴 **Supabase `public` 스키마의 default ACL 이 `anon` 에게 테이블 `arwdDxtm`(ALL) · 함수 `X`(EXECUTE) 를 준다**(2026-09-12 `pg_default_acl` 실측 · 주인이 `postgres`·`supabase_admin` **둘**). **53개가 anon ALL 이었던 원인이 이것**이고, 테이블마다 `REVOKE` 만 하면 **다음 `migrate` 한 번에 되돌아간다** — `alter default privileges ... revoke` 를 함께 해야 한다. 🔴 그리고 **`from anon` 만으로는 함수가 안 닫힌다**: Postgres 는 함수 생성 시 `PUBLIC` 에 EXECUTE 를 기본 부여하고 `anon` 은 PUBLIC 의 일원이다 → `revoke ... from public, anon, authenticated` 로 적어야 한다 (실제로 트리거 함수 하나가 이 차이로 열려 있었다) |
| **V37** | ✅ **53개 테이블 소유자는 전부 `postgres` 이고 `postgres` 는 `rolbypassrls = true`**(`anon` 은 `false`). 즉 **RLS 를 켜도 Django 는 영향받지 않는다** — ADR-SC-0010 ⑥ 의 "정책 없이 RLS 만 켜면 앱이 죽는다" 는 **anon 경로에만** 해당한다. 그래서 v2.0 유산을 **정책 0개로 전면 차단**할 수 있었다 |
| **V38** | 🔴 **`pg_column_size` 는 STABLE 이라 CHECK 제약에 쓸 수 없다**(`provolatile='s'` 실측). payload 크기를 제한하려면 **`octet_length(payload::text)`** 를 쓴다 — `octet_length` 도 `jsonb_out` 도 IMMUTABLE 이다. 적용 전에 걸렀기에 실패하지 않았다 |
| **V39** | ✅ **원장 RLS·RPC 를 `anon` 역할로 실동작 검증했다**(2026-09-12 · 트랜잭션 후 롤백 · 11항목 전부 통과) — anon 직접 INSERT 거부 · passcode 해시 SELECT 거부 · `passcode_params` 가 **digest 를 주지 않음** · 맞는 passcode append 1건 · 재전송 **0건(멱등)** · 틀린 passcode 거부 · 다른 조 이벤트 거부 · payload 의 `passcode_hash` CHECK 거부 · `at` 형식 CHECK 거부 · 🔴 **`postgres`(BYPASSRLS)조차 DELETE 거부**(트리거) |
| **V40** | ⚠️ **Supabase advisor 에 `ERROR` 는 0건이나 `WARN`·`INFO` 가 남는다 — 전부 의도다.** ① `rls_enabled_no_policy` 54건 = 정책 0개가 곧 전면 차단(V37) ② `anon/authenticated_security_definer_function_executable` 각 4건 = RPC 가 **쓰기 관문**이라 anon 이 부를 수 있어야 한다(ADR-SC-0011 ⑤). 🔒 **이 둘을 "고쳐야 할 것" 으로 보고하지 않는다** |
| **V41** | 🔴 **읽기 전용 검증 RPC 가 없다.** `workspace_append` 는 **빈 배열을 passcode 검사 *앞에서*** 0건으로 돌려보내므로(`if jsonb_array_length(p_events) = 0 then return 0`) 빈 append 로는 passcode 를 확인할 수 없다. → 참가 검증은 **그 조의 `team.created` 를 그대로 다시 보내서** 한다: passcode 는 삽입 전에 검사되고 `on conflict do nothing` 이라 **0건 쓰인다**. 🔒 5번째 마이그레이션을 만들지 않았다 — 이미 있는 멱등성으로 되는 일에 DB 표면을 늘리지 않는다. `verify` 를 **저장소 계약**으로 올려 세 구현이 각자 답한다 |
| **V42** | 🔴🔴 **마스터(개발자) 쓰기 경로가 DB 에 없다.** 화면에는 마스터가 남의 조를 보관·복구하는 기능이 있는데(`ADMIN_PASSCODE_HASH`), 마스터는 그 조의 passcode 를 갖고 있지 않고 **가질 수도 없다**(해시만 저장한다). 지금까지 된 이유는 원장에 쓰기 관문이 없었기 때문이다. 🔒 우회하지 않았다 — `SupabaseStore.append` 는 자격증명이 없으면 무엇을 해야 하는지 말하고 멈춘다. **앱을 Supabase 로 돌리기 전에** 골라야 한다(ADR-SC-0011 ⑪ · 기본값은 "보관·복구를 조 안으로 되돌린다") |
| **V43** | ✅ **HF 원장에 실제 팀 데이터가 없었다**(2026-09-12 실측 · `team-workspace` 파일 5개 · 이벤트 3건 전부 같은 날 06:55 스모크 테스트 `test_team`/`테스트1`). 그래서 passcode 를 원장 밖으로 뺄 때 **마이그레이션 경로를 만들지 않았다** — 옛 형식은 **읽히되** `fold.anomalies` 가 "옛 형식 · 참가할 수 없다" 고 말한다. 🔒 읽기를 막으면 한 줄이 팀 전체 화면을 죽인다 |
| **V45** | 🔴 **Supabase GitHub 연동(자동 마이그레이션 적용)을 쓰지 않는다**(2026-09-12 결정 · 사용자 스크린샷으로 확인). Settings → Integrations → GitHub 의 `Deploy to production` 은 **연결된 저장소의 `supabase/migrations/` 를 프로덕션 DB 에 자동 적용**한다. 이유 넷 — ① 절대 제약 6(CI/CD 를 필수 경로에 두지 않는다) ② **이 저장소는 main 직커밋**이라 커밋 = 즉시 프로덕션 DB 변경이고 PR 게이트가 0(그 기능은 PR 을 전제한다) ③ 후보로 뜨는 `devlee328288/stock-coin-trade` 는 **force push 되는 GitLab 미러**라 정본이 아니고 갱신 시점을 우리가 못 정한다 ④ Free 는 **preview branch 가 없고**(Pro 필요) 원장은 append-only 트리거라 되돌리기가 특히 비싸다. ⚠️ 실측 `list_migrations` 5건 중 `20260813012735_enable_pgcron_and_vector` 는 **저장소에 파일이 없다** — 자동 적용을 켠다면 이 불일치를 먼저 봐야 한다 |
| **V46** | 🔴 **Supabase 의 "Install Vercel integration" 도 쓰지 않는다**(2026-09-12). 그 연동은 *"환경변수를 최신으로 유지"* — 즉 `VERCEL.md` 3.2 표를 **자동으로 덮어쓴다.** ① **얻는 것이 0** — `backend/` 는 `SUPABASE_*` 를 **한 줄도 읽지 않는다**(실측 0건). `SUPABASE_URL`·`ANON_KEY` 를 쓰는 쪽은 Streamlit(v3.0)이고 **거기는 Vercel 이 아니다** ② 🔴 **이름이 겹쳐 표를 깨뜨린다** — 연동은 `POSTGRES_HOST`·`USER`·`PASSWORD` 를 다이렉트 형태로 넣고 `POSTGRES_DATABASE`(우리는 `POSTGRES_DB`)를 쓰며 `POSTGRES_PORT`·`POSTGRES_PGBOUNCER` 는 안 준다 → 풀러 유저명이 덮이면 `Tenant or user not found`, 붙어도 `PGBOUNCER` 가 빠져 **트래픽 붙은 뒤 간헐 500**(VERCEL.md 2.3). 🔒 부분 덮어쓰기가 전부 덮어쓰기보다 나쁘다 ③ 🔴 `SUPABASE_SERVICE_ROLE_KEY`·`JWT_SECRET` 을 Vercel 환경에 심는다(ADR-SC-0011 ⑤ 금지) ④ 절대 제약 6. ⚠️ 주입 목록은 버전마다 달라졌으니 install 화면 목록을 먼저 읽는다 |
| **V44** | ⚠️ **루트 `.venv` 에 `ruff` 가 없다**(2026-09-12 · `requirements-dev.txt` 에도 없다). `pyproject.toml` 의 line-length 100 설정은 에디터용이고 **로컬 lint 단계가 없다.** 규약 검사는 지금 `awk 'length>100'` 수준으로만 가능하다 — 넣을지는 별도 결정 |
| **V13** | `gh` 활성 계정이 **`dev-dongwon05253`**(개인·Flagged)과 **`devlee328288`**(팀·공개) 사이를 오간다 |
| **V29** | 🔴 **HF 토큰 이름이 역할과 어긋나 있었다**(2026-09-11 실측) — `HUGGINGFACE_ACCESS_TOKEN` 에 들어 있던 값이 fine-grained 토큰이고 **org `stock-coin-trade` 전체에 `repo.write`** 를 가진다. 이름이 "ACCESS" 라 앱용 read 칸으로 읽히기 쉬웠고, 그대로 Streamlit Secrets 에 넣었다면 **쓰기 권한이 팀에 넘어간다.** → `HF_TOKEN_WRITE`(배치·로컬 `.env` 만) / `HF_TOKEN_READ`(앱) / `HF_TOKEN_NOTES_WRITE`(M13) 로 갈랐다. 🔴 **`HF_TOKEN_READ` 는 아직 없다** — 앱 배포 전에 `sector-scores` **read 전용** fine-grained 토큰을 따로 발급해야 한다 |
| **V30** | ✅ **parquet 바이트가 결정적이다**(pandas 3.0.5 + pyarrow 25.0.1 실측) — 같은 프레임을 두 번 써도, 라운드트립 후에도 sha256 이 같다(`created_by` 는 버전 문자열이라 고정). 🔴 다만 `fetched_at` 이 **빌드 시각**이라 집계를 다시 돌리면 값이 그대로여도 15개월치 샤드가 전부 "바뀐 파일"이 된다. 그래서 멱등성 판정은 `fetched_at` 을 **뺀** 내용 해시(`content_sha256`)로 하고, 건너뛴 파일의 MANIFEST 항목은 **원격 것을 그대로 이어받는다**(안 그러면 MANIFEST 가 올리지도 않은 바이트를 서술한다) |
| **V31** | 🔴 **ETF 가 1개뿐인 섹터가 둘 있다** — `telecom`·`transport`. 그 섹터에서 `etf_shares_sum`·`etf_nav_sum`·`etf_value_sum` 은 집계가 아니라 **그 ETF 의 KRX 값 그 자체**다(ADR-SC-0006 ② 가 종가로 든 예와 같은 문제). → `etf_nav_sum` 은 **내보내지 않고**(자금흐름 축이 쓰지 않기로 이미 정해져 있다), `etf_shares_sum` 은 **`etf_shares_idx_bp`(기준일 10000)로 지수화**해 내보낸다. 20일 비율 오차 1.6e-05 로 축 계산에 무해하다. ⚠️ **`etf_value_sum` 만 수준 그대로 나간다** — 유동성 게이트가 원 단위를 요구해서다. M7 이 `liquidity_ok` 를 `score_daily` 에 담으면 허용목록에서 한 줄 빼면 된다 |
| **V33** | 🔴 **횡단면 표준화는 공통 상수를 소거한다 — V28 의 답이 여기서 나온다.** `r20_mkt` 는 모든 섹터에 같은 값으로 들어가므로, 중앙값을 빼는 순간 기준선이 사라진다. 실측(5985행): 기준선을 시총가중↔동일가중으로 바꾸면 **raw 는 4354bp(43.5%p) 움직이는데 z 는 98bp · 점수 40bp · 순위가 달라진 행 18/5985** 뿐이고, 그 잔차마저 전부 **bp 양자화의 반올림**이다(`raw_bp` 가 같은 1281행에서는 z 차이 **0bp**). → 그래서 기준선은 **점수가 아니라 설명** 때문에 고른다. 축 분해 표에 "시장 대비"라고 적으려면 기준선이 실제 시장이어야 하고, 시총가중은 사실상 "반도체 상위 2종 대비"다. **`eqw_idx_bp`(동일가중)를 쓴다** |
| **V34** | 🔴 **중앙값절대편차(MAD)가 0 이 되는 날이 실재한다** — 폭(B) 축에서 **266일 중 45일**(17%). 섹터 구성종목이 3~8개뿐이라 `breadth_up_bp` 가 극도로 이산적이다(0% 18.8% · 100% 18.3% · 하루 21섹터의 고유값 중앙값 10개). 🔴 그 45일에 중앙값과 **다른** 섹터가 늘 2~10개였다 — 0개인 날은 **하루도 없다.** 그러니 "변별 불가"가 아니고, 축을 버리면 반드시 정보를 잃는다. → MAD 가 0 이면 **평균절대편차(×1.2533 = √(π/2))로 한 단 내린다.** 둘 다 0 이면 그때만 축을 쓰지 않는다. 🔒 지어내는 것이 아니라 **척도 추정량을 바꾸는 것**이고, `axes_degraded` 가 행마다 기록한다 |
| **V35** | 🔴 **스코어링의 창은 *영업일* 기준이고, 창이 온전할 때만 계산한다.** `aggregate.lagged_return` 은 *관측* 기준인데(값 없는 날은 창이 달력상 길어진다) 스코어링은 그럴 수 없다 — M 축이 `r20_섹터 − r20_시장` 이라 두 계열이 **같은 달력 구간**을 봐야 뺄셈이 뜻을 갖기 때문이다. 섹터가 3일 쉬면 관측 기준으로는 23영업일과 20영업일을 빼게 된다. → 창 `[T−k, T]` 에 관측이 하나라도 빠지면 그 축은 `None`. 전일값으로 메우면 결측을 0 으로 채우는 것과 같다. ★ 이 판정에 **시장 계열의 관측 신호**가 필요해 `market_daily` 에 `eqw_ret_1d_bp` 를 새로 넣었다 |
| **V36** | ✅ **`etf_value_sum` 을 게시 허용목록에서 회수했다**(V31 이 예고한 대로). 유동성 판정을 M7 이 로컬에서 끝내고 `score_daily.liquidity_ok`(bool) 로만 넘기므로, ETF 1개 섹터(`telecom`·`transport`)에서 그 값이 원천과 같아지던 **아는 노출**이 사라졌다. 실측 유동성 미달은 `telecom` 76일뿐(5586칸 중 1.36%) |
| **V32** | ✅ **`assert_private` 이 실서버에서 막는다**(2026-09-11 · 공개 dataset 을 `--repo-id` 로 지정해 확인) — 종료코드 1, 커밋 0건. `create_repo(exist_ok=True)` 가 public 대상에도 성공하는 것과 별개다. 🔒 확인을 위해 `sector-scores` 를 공개로 바꾸지 **않았다** |

### 이력·저장소

| # | 사실 |
|---|---|
| **V9** | 옛 이력에 강사님 시크릿 실재 — `k8s/{base,eks}/secret.yaml` 의 `CMC_API_KEY: 89a4ffe3-…` 외 DB 비밀번호 4종. `ghp_`/`glpat-`/`AKIA`/`sk-ant-`/JWT 는 **0건** |
| **V10** | 강사님 파일과 본인 파일이 **최상위 경로 단위로 분리**된다 |
| **V11** | GitLab push-mirror 는 Free 에서 Push 방향 지원. **force push 로 동작** |
| **V18** | `.github/workflows/trigger-production-deploy.yml` 은 **강사님 커밋**이고 push 마다 `edumgt/investment-analysis` 에 배포 dispatch 를 쏜다 → M1 에서 `.github/` 삭제로 정리 |

### 대회 규정

| # | 사실 |
|---|---|
| **V14** | ⚠️ 옛 판단 — "규정 전 항목 확인 불가". 여전히 **규정에 설계를 매달지 않는다** |
| **V37** | ✅ **대회 성격을 사용자에게 확인했다**(2026-09-11) — "일반적인 투자 HTS 모의투자와 비슷한데 **ETF 섹터를 지정해서 포트폴리오 관리 능력까지** 본다". 즉 ① **ETF 가 실제 매매 단위**이고 ② **포트폴리오 운용도 평가 항목**이다. 🔴 ①이 M7 의 미결을 풀었다 — 섹터 ETF 점수가 곧 매매 후보 랭킹이므로 **종목 단위 점수는 M7 범위 밖**이다(아래 M7 결정 ④). ②는 **v3.0 범위 결정으로 남아 있다** — 보유 비중·집중도·리밸런싱을 다룰지는 M8 착수 전에 정한다 |

---

## 3. 확정된 설계 — 다시 논의하지 않는다

- **섹터 = 2계층 × 2렌즈**. GICS 11 대분류(상위) + 한국 테마 ETF 21개(하위).
  각 섹터를 ETF 렌즈와 구성종목 렌즈로 본다. 🔒 **갈라지면 갈라진 채로 보여준다**
- **스코어링 4축** — 모멘텀 35 / **자금흐름 30** / 폭 20 / 밸류 15
  - 자금흐름 = **ETF 상장좌수(`LIST_SHRS`) 20일 변화율만.** 순자산총액을 쓰지 않는다
    (= 좌수 × NAV 라 NAV 변화를 모멘텀 축과 이중 계산하게 된다)
  - 🔒 **거래대금은 점수에 안 넣는다**(V15). **유동성 게이트**로만 — 일평균 1억 미만은 경고 배지
  - 🔒 **관심(뉴스·공시) 축을 만들지 않는다.** 부호가 섞인 지표는 축이 될 수 없다
- **정규화는 중앙값·MAD** (섹터 21개뿐이라 평균·표준편차는 한 섹터 급등에 무너진다).
  저장은 `score_bp` 정수 양자화
- **HF 에는 파생값만.** 종목·ETF 원천은 로컬 `data/raw/`(gitignore)
- **구성종목은 `sectors.yaml` 에 사람이 손으로 적는다**(V5). 🔒 **모든 섹터에 `note` 필수**
- **준실시간 = KIS**, 일별 = KRX Open API
- ~~Supabase·Vercel 을 쓰지 않는다~~ → **2026-09-12 개정**(ADR-SC-0010):
  **Streamlit = 팀용 정본 · Django+Vercel = 확장·쇼케이스**로 **둘 다 유지**한다.
  쓰기 상태(팀 원장)는 **Supabase**, 파생 집계·점수는 **HF** — 이중 보관
- **저장소 public(코드만) + 앱 private(뷰어 6명)**

### M7 에서 정해진 것 (2026-09-11) — 되짚지 않는다

① **M 축 기준선 = `eqw_idx_bp`(동일가중).** 근거는 점수가 아니라 **설명**이다 → V33
② **MAD 가 0 이면 평균절대편차로 한 단 내린다.** 축을 버리지 않는다 → V34
③ **창은 영업일 기준 · 온전할 때만 계산.** 아니면 `None` → V35
④ 🔴 **종목 단위 점수를 만들지 않는다.** 대회의 매매 단위가 ETF 이기 때문이다(V37).
   재료는 있으나(구성종목 125개 · `aggregate.py:272 stock_series`) 4축이 종목에
   전이되지 않는다 — B 는 무의미하고 F 는 전이 불가(종목 상장주식수 증가는 **희석**이고,
   외국인·기관 순매수는 KRX 에 없다 · V5). 종목은 **근거 화면의 재료**로만 쓴다(M10·M11)
⑤ **점수 척도** — `score_bp` = `Σ w·z / Σ w` × 10000, 범위 **[−30000, +30000]**(±3σ).
   계획서의 "0~10000" 은 부정확한 서술이었다. 부호가 있다
⑥ **프리셋 3종만 저장한다.** 슬라이더는 앱이 `*_z_bp` 로 그 자리에서 가중합만 다시 한다
⑦ **결측 축은 가중치를 재정규화한다.** 1축만 선 섹터의 점수 = 그 축의 z 다

**파이프라인은 이제 네 걸음이다** — 수집 → 집계 → **채점** → 게시.
각 걸음은 한 가지만 하고, 앞 셋은 네트워크를 부르지 않는다.

```bash
python -m batch.fetch_daily --days 425      # data/raw/  (유일하게 네트워크를 쓴다)
python -m batch.build_sector_daily          # → sector_daily · market_daily
python -m batch.build_scores                # → score_daily      ★ M7 에서 생겼다
python -m batch.publish --date 20260910     # → HF private
```

전체 계획: `~/.claude/plans/wondrous-conjuring-horizon.md`
결정 근거: [`docs/decisions/`](docs/decisions/) ADR-SC-0005 ~ 0008
⚠️ **ADR-SC-0001·0002 는 폐기됐다.** 그 문서를 근거로 삼지 마라.

---

## 4. 마일스톤 — 한 세션 = 하나

| M | 이름 | 상태 |
|---|---|---|
| **M0** | 전환 결정 문서화 (ADR 0005~0008 · AGENTS · CLAUDE · 이 파일) | ✅ **2026-09-11 완료** |
| **M1** | **이력 재시작** — bundle 백업·verify → orphan → 강사님 경로 제거 → force push → 부모 서브모듈 갱신 | ✅ **2026-09-11 완료** |
| M2 | 배선 — 원격 재구성 · GitLab mirror · Vercel 연동 해제 · "Hello" 앱 배포 | 🔄 **거의 끝났다**(2026-09-11 확인) — GitLab 보호·미러(`2df953c` 반영) ✅ · 미러 Issues·Projects·Wiki 끄기 ✅ · **Streamlit 배포 + private ✅** (https://gic-stock-coin-trade-practice.streamlit.app/ → `/-/login` 303). 🔴 남은 것: **Vercel 연동 해제**(EST 저장소 webhook 0건이나 대시보드 미확인) · 눈으로만 확인되는 둘 — **Python 3.12 선택 여부**(배포 후 변경 불가) · **뷰어 6명 등록** (→ `deploy.md`) |
| M3 | KRX 어댑터 (`sources/krx_openapi.py`) | ✅ **2026-09-11 완료** — 실원천 1168행 파싱 확인 · 테스트 40건 |
| M4 | 섹터 정의 (`sectors.yaml` 21섹터 · `validate_config.py`) | ✅ **2026-09-11 완료** — ETF 40건·구성종목 125건 **전부 원천 실재 확인** · 테스트 102건 |
| M5 | 집계 (`sector_daily` 생성) | ✅ **2026-09-11 완료** — 원천 909파일(76MB gzip · `data/raw/`) · **285영업일**(20250714~20260910) · `sector_daily` 5985행 · `market_daily` 285행 · 테스트 141건 |
| M6 | HF 파이프라인 (private 게이트 · `publish.py`) | ✅ **2026-09-11 완료** — `sector-scores` **private** · 파일 36개 797KB · 단일 커밋 35건 → **2회차 업로드 0건** · 테스트 234건 |
| M7 | 스코어링 + 골든 테스트 | ✅ **2026-09-11 완료** — `sector/scoring.py` · `batch/build_scores.py` · 골든 23건(스냅샷 3 + 성질 20) · 게이트 검사 11개 · 테스트 **266건** · HF 51파일 → **2회차 0건** |
| M8 | UI 기반 (`theme.py` · Pretendard · 협업 원장 · Supabase) | 🔄 **진행 중** — F-4 팔레트를 `config.toml` 로(`ff4e70e`) ✅ · **#7.5 Supabase 스키마+RLS ✅ 2026-09-12** (ADR-SC-0011 · 마이그레이션 4건 · V39) · **#7.7 코어를 새 스키마에 맞춤 ✅ 2026-09-12** (passcode 가 원장 밖 · `SupabaseStore` · 테스트 375→415건 · 의존 증가 0). 남은 것: #7.6 Django/Vercel 공존 실증 · #8 근거 첨부 · 🔴 **앱을 Supabase 로 돌리기 전에 V42(마스터 경로) 결정** |
| M9 | 랭킹 페이지 | ⬜ |
| M10 | **섹터 상세(근거)** ★ 워터폴 · 축 분해 · 원천 시계열 | ⬜ |
| M11 | KIS 준실시간 + 종목 페이지 | ⬜ |
| M12 | 공시·뉴스 (요약 0) | ⬜ |
| M13 | 팀 노트 (HF `team-notes`) | ⬜ |
| M14 | 운영화 (읽는 법 · 데이터 상태 · `run_daily.ps1`) | ⬜ |

### M1 을 시작하기 전에 — 반드시

```bash
git bundle create ../../stock-coin-trade-fork-history-20260911.bundle --all
git bundle verify ../../stock-coin-trade-fork-history-20260911.bundle   # verify 까지 해야 백업이다
```

🔴 **부모 모노레포 `projects/` 서브모듈이 `9b90f49` 를 가리킨다.**
orphan 후 `projects` → 부모 순서로 새 gitlink 를 커밋하지 않으면 서브모듈이 깨진다.

---

## 4.5 미조치 — 알고 있으나 아직 안 고친 것

### `sectors.yaml` 의 `liquidity_warning` 두 줄이 실측과 어긋난다 (2026-09-11 측정)

M7 이 유동성을 **20영업일 평균 거래대금 ≥ 1억**으로 실제 판정하게 되면서 확인됐다.
285영업일(판정 가능 266일) 전수 —

| 섹터 | yaml | 실측 미달일 | 최근 60일 |
|---|---|---|---|
| `retail_ecommerce` | `true` | **0일** | 0일 |
| `telecom` | `true` | 76일 | **0일** |

- `retail_ecommerce` 는 **하루도 미달한 적이 없다** — 경고가 그냥 틀렸다.
- `telecom` 은 과거에 76일 미달했으나 **최근 60일은 전부 통과**했다.

🔴 고치는 방향을 먼저 정해야 한다. 손으로 적은 `liquidity_warning` 이 이제
**중복 정보**다 — 같은 질문에 데이터가 더 정확히 답한다. 지우고 `liquidity_ok`
하나로 갈지, 아니면 "사람이 아는 구조적 사유"를 남길 자리로 둘지 판단이 필요하다.
지금은 `validate_config` 가 warn 2건으로 알리기만 한다 (error 0건).

---

### Supabase 웹 설정 — 무엇을 켜고 무엇을 끄나 (2026-09-12)

| 화면 | 설정 | 어떻게 |
|---|---|---|
| Settings → **Integrations → GitHub** | `Deploy to production` | 🔴 **끈다** (V45) |
| Settings → **Integrations → Vercel** | `Install Vercel integration` | 🔴 **누르지 않는다** (V46). 환경변수는 `VERCEL.md` 3.2 를 보고 손으로 |
| Settings → **API Keys** | anon / publishable 키 | ✅ 복사해 루트 `.env` + Streamlit Secrets |
| Settings → **API Keys** | `service_role` / `sb_secret_` | 🔴 **어디에도 넣지 않는다.** 코드가 거부한다 |
| Settings → **General** | Project URL | ✅ `SUPABASE_URL` 에 넣는다 |
| Database → **Migrations** | 적용 내역 | 📖 읽기만. 적용은 한 건씩 손으로 |

🔒 **Vault·Log Drains·Add-ons 는 건드리지 않는다** — 쓰지 않는 기능을 켜면 Free 한도
   (DB 500MB)만 깎인다.

---

## 5. 사용자가 준비해 주셔야 할 것

| | 무엇 | 언제 |
|---|---|---|
| ✅ | **KRX Open API 키** — `etp/etf_bydd_trd`(1168행) · `sto/stk_bydd_trd`(943행) · `sto/ksq_bydd_trd`(1823행) **셋 다 승인 확인됨**(2026-09-11 HTTP 200 · V23). **지수(`idx/*`) 5종도 승인 확인됨**(V24). 🔴 미확인 경로가 없다 | M3 ✅ · M4 ✅ · M5 ✅ |
| ⬜ | devlee328288 GitHub PAT (fine-grained · 대상 저장소 1개 · Contents R/W) | M2 전 |
| 🔺 | **HF 토큰** — 배치용 `HF_TOKEN_WRITE` ✅ 있다(M6 에서 사용). 🔴 **`HF_TOKEN_READ`(= `sector-scores` read 전용)가 아직 없다** — 앱이 데이터를 읽으려면 필요하다. `team-notes` write 는 M13 전 | M8 전 |
| ⬜ | 🔴 **Supabase 키 둘** — 루트 `.env` 에 `SUPABASE_URL=https://sgbhrahtewojmicwmxxu.supabase.co` 와 `SUPABASE_ANON_KEY=<anon / publishable>` (2026-09-12 확인: **둘 다 없다**). 🔴 `service_role`·`sb_secret_` 를 넣지 않는다 — 코드가 거부한다. 배포 시 같은 둘을 Streamlit Secrets 에도 넣는다 | 앱을 Supabase 로 돌리기 전 |
| ⬜ | `.env.example` 에 위 두 줄의 **이름만** 추가 — 🔒 에이전트 권한 설정이 `.env*` 읽기를 막아 두어(`.claude/settings.json` deny) 내가 손대지 않았다 | 위와 같이 |
| ⬜ | 🔴 **Supabase → Settings → Integrations → GitHub 의 `Deploy to production` 을 끈다** (연동돼 있으면 **연동 해제**). 근거는 V45 — main 직커밋이라 커밋이 곧 프로덕션 DB 변경이 되고, 대상 저장소가 force push 미러다 | 지금 |
| ⬜ | 🔴 같은 화면의 **Vercel 연동은 설치하지 않는다**(V46). Vercel 환경변수는 `VERCEL.md` 3.2 표를 보고 **손으로** 넣는다 — 특히 `POSTGRES_PGBOUNCER=1` 과 풀러 유저명 `postgres.sgbhrahtewojmicwmxxu` | #7.6 전 |
| ⬜ | 팀원 6명 이메일 (Streamlit private 앱 뷰어) | M2 전 |
| ⬜ | DART 인증키 · 네이버 API HUB 키 | M12 전 |
| ⬜ | 🔴 **강사님께** — 이력의 `CMC_API_KEY: 89a4ffe3-…` 가 `edumgt/stock-coin-trade` 에 여전히 공개돼 있음을 알리고 키 폐기 권유 | 아무 때나 |
| ⬜ | 동아리 섹터 규칙이 정해지면 `sectors.yaml` 에 반영 | 나올 때 |

**이미 있는 것**: `KRX_API_KEY`·`KRX_ID`·`KRX_PW`·`DART_API_KEY`·`ECOS_API_KEY`·
`HF_TOKEN_WRITE`(← 옛 `HUGGINGFACE_ACCESS_TOKEN`, V29)·`NAVER_API_CLIENT_ID`·`KIS_*` 가 루트 `.env` 에 있다
(2026-09-11 중복 9건 정리 완료 · 72→66줄).

---

## 6. 완료 조건 (DoD)

- [x] ADR-SC-0005~0008 이 `docs/decisions/` 에 있다
- [x] ADR-SC-0001·0002 에 폐기 표시가 있다
- [x] `AGENTS.md`·`CLAUDE.md`·이 파일이 v3.0 을 서술한다
- [x] 루트 `.env` 에 중복 키가 없다
- [x] bundle 백업이 있고 `verify` 를 통과한다 (M1) — 13refs · `complete history` · 모노레포 루트 · `*.bundle` gitignore
- [x] `git log` 가 1커밋이고 강사님 코드가 없다 (M1) — `f9f3fe4` · 258파일 · `k8s/`·`.github/` 0건
- [~] 부모 모노레포 `git status` (M1) — gitlink 는 갱신·push 완료(`projects` 6b84131 · 모노레포 980601e).
      ⚠️ `projects` 가 ` m` 으로 남는다 — **이 저장소와 무관한** 다른 서브모듈 3개
      (`data-service`·`investment-dashboard`·`quant-contract`)가 커밋되지 않은 채 앞서 있어서다.
      각 저장소 담당 세션에서 정리한다. stock-coin-trade gitlink 자체는 깨끗하다
- [ ] 팀원이 URL 로 화면을 본다 (M2)
- [ ] **`cd backend && .venv/bin/pytest` 23건이 계속 통과한다** (M1·M7·M14 전부) — M1 ✅ · M3 ✅ · M4 ✅ 23건 통과(2026-09-11)
- [x] **21섹터 전부에 `note`(왜 이렇게 묶었나)가 있다** (M4) — 검증기가 20자 미만을 `error` 로 잡는다.
      빈 GICS 대분류 2개(Energy·Real Estate)도 `empty_reason` 으로 왜 비었는지 말한다
- [x] **ETF 40건 · 구성종목 125건이 전부 원천 유니버스에 실재한다** (M4) — `etf_bydd_trd` 1168종 ·
      `stk_bydd_trd`+`ksq_bydd_trd` 2766종과 대조. 🔴 기억으로 적은 6건이 실제로 어긋나 있었다
      (사명 변경 4 · 유니버스에 없음 2) — 검증 없이는 그대로 굳었다
- [x] **스냅샷이 없으면 "통과"라고 말하지 않는다** (M4) — `validate_config` 가 종료코드 **2**(검증 불가)를
      1(검증 실패)과 구별한다. `--allow-no-universe` 를 명시해야 형식만 본다
- [x] **KRX 원천이 저장소에 없다** (M3) — `data/` 전체 untracked 확인 · `--probe` 는 쓰기 전에
      `git check-ignore` 로 **경로가 실제로 무시되는지 물어보고** 아니면 거부한다
- [x] **최근 1년 `sector_daily` 가 로컬에 있고 원천은 `data/raw/` 를 떠나지 않았다** (M5) —
      285영업일 × 21섹터 = 5985행 · `git status` 에 `data/` 0건 · `.gitignore:25 /data/` 가 둘 다 막는다
- [x] **룩어헤드가 실데이터에서 확인됐다** (M5) — `data[:T+40]` 의 `row(T)` 와 `data[:T]` 의 값이
      5166행 전부 일치. 알고리즘이 날짜를 **오름차순 한 번만** 훑으므로 성질이 구조에서 나온다
- [x] **저장 자료형에 float 가 없다** (M5) — nullable `Int64` 를 dataclass 주석에서 끌어낸다(V26)
- [x] **HF `sector-scores` 에 종목·ETF 원천 파일이 한 개도 없다** (M6) — 서버 파일 목록 36개를
      `bydd_trd`·`raw`·`OutBlock` 으로 대조해 0건. 게시본을 되읽어 **열 계약**까지 확인했다
- [x] **두 번째 실행에서 업로드 0건** (M6) — 게시 경로가 벽시계를 읽지 않는다(`--date` 와 데이터뿐).
      정적 검사 `test_게시_경로에_벽시계가_없다` 가 그 계약을 지킨다
- [x] **public 이면 한 파일도 올라가지 않는다** (M6) — 가짜 API 2겹 + **실서버**(공개 dataset 을
      `--repo-id` 로 지정)로 확인. 🔒 `sector-scores` 를 공개로 바꿔 보지 **않았다** — 잠깐이라도
      공개하는 순간이 바로 ADR-SC-0006 이 막는 상태다
- [x] 모든 화면 하단에 "한국거래소 통계정보" 출처가 보인다 (M2 부터) — `AppTest` 로 렌더 확인. 🔒 `try/finally` 라 본문이 죽어도 남는다(예외 주입해 실증)
- [ ] 개발을 모르는 팀원이 "읽는 법" 1장만 보고 스스로 판단한다 (M14)

---

## 7. 컨벤션 요약 (전문은 `AGENTS.md` 4장)

- Python 3.12 · 4 spaces · ruff line-length 100 · 한국어 주석·문서
- 금액 정수(원) · 비율 bp 정수 + `Decimal` · **`float` 금지**
- 영업일 키는 `bas_dd`(`"YYYYMMDD"` 문자열 — 사전순 = 날짜순)
- KST 로 사고, UTC 로 저장
- 커밋: `<scope>: <동사원형 요약>` · 문서는 개조식("-다")
- Mermaid `flowchart`만 (`C4Context` 금지)
